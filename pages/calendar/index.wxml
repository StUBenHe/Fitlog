<view class="page">
  <swiper class="swiper" current="{{currentIndex}}" circular>
    <swiper-item wx:for="{{days}}" wx:key="date">
      <view class="card">

        <!-- 日期 -->
        <view class="date">
          <text class="day">{{item.date}}</text>
          <text class="weekday">{{item.weekday}}</text>
        </view>

        <!-- 模块容器 -->
        <view class="modules">

          <!-- 饮食模块 -->
          <view class="module diet" bindtap="goDiet" data-date="{{item.date}}">
            <view class="module-header">
              <text class="module-title">🍽 饮食</text>
            </view>
            <view class="module-content">
              <text wx:if="{{item.diet.status === 'empty'}}" class="placeholder">
                未记录
              </text>
              <text wx:else class="value">
                摄入 {{item.diet.calories}} kcal
              </text>
            </view>
          </view>

          <!-- 训练模块 -->
          <view class="module workout" bindtap="goWorkout" data-date="{{item.date}}">
            <view class="module-header">
              <text class="module-title">🏋️ 训练</text>
            </view>
            <view class="module-content">
              <text class="value">{{item.workout.type}}</text>
              <text
                wx:if="{{item.workout.calories}}"
                class="sub"
              >
                消耗 {{item.workout.calories}} kcal
              </text>
            </view>
          </view>

        </view>
      </view>
    </swiper-item>
  </swiper>
</view>
